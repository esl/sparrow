use Mix.Config

# Please refer to README.md to get more detail of how this file should look like.
# You can also look @ `config/prod.example.exs` to get rough idea how this should work.

config :sparrow,
  fcm: [
    # Your FCM pool definitions here
  ]

config :sparrow,
  apns: [
    # Your FCM pool definitions here
  ]
